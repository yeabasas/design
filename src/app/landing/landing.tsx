import { RoutesCart } from "@design/cart";
import { Input } from "@design/shared/ui";

/* eslint-disable-next-line */
export interface LandingProps {}

export function Landing(props: LandingProps) {
  return (
  <RoutesCart/>
  );
}

export default Landing;
